---
import { TESTIMONIALS } from '@/consts/testimonials';
import Icon from '@/components/Icon.astro';
---

<section id="testimonios" class="bg-white py-20">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="mb-16 text-center">
      <h2 class="mb-4 text-3xl font-bold text-gray-900 md:text-4xl">
        Lo que dicen nuestros clientes
      </h2>
      <p class="mx-auto max-w-3xl text-xl text-gray-600">
        La satisfacción de nuestros clientes es nuestra mayor recompensa. Aquí
        puedes leer sus experiencias y comentarios reales.
      </p>
    </div>

    <div class="grid grid-cols-1 gap-8 md:grid-cols-2 lg:grid-cols-3">
      {
        TESTIMONIALS.map(testimonial => (
          <div class="rounded-xl border border-gray-100 bg-gradient-to-br from-white to-gray-50 p-6 shadow-md transition-all duration-300 hover:shadow-lg">
            <div class="mb-4 flex items-center">
              <img
                src={testimonial.avatar}
                alt={testimonial.name}
                class="mr-4 h-12 w-12 rounded-full object-cover"
              />
              <div>
                <h4 class="font-semibold text-gray-900">{testimonial.name}</h4>
                <p class="text-sm text-gray-600">{testimonial.service}</p>
              </div>
            </div>

            <div class="mb-4 flex items-center">
              {[...Array(testimonial.rating)].map(_ => (
                <Icon name="star-filled" class="size-5 text-yellow-400" />
              ))}
            </div>

            <div class="relative">
              <Icon
                name="quote"
                class="absolute -top-2 -left-2 h-8 w-8 text-blue-200"
              />
              <p class="pl-6 leading-relaxed text-gray-700">
                {testimonial.comment}
              </p>
            </div>
          </div>
        ))
      }
    </div>

    <div
      class="mt-16 rounded-2xl bg-gradient-to-r from-green-500 to-blue-600 p-8 text-center text-white"
    >
      <h3 class="mb-4 text-2xl font-bold">
        ¿Quedaste satisfecho con nuestro servicio?
      </h3>
      <p class="mb-6 text-green-100">
        Ayúdanos a seguir mejorando dejando tu comentario y compartiendo tu
        experiencia con otros clientes.
      </p>
      <div class="flex flex-col justify-center gap-4 sm:flex-row">
        <a
          href="https://instagram.com/techrepairpro"
          target="_blank"
          rel="noopener noreferrer"
          class="rounded-lg bg-white px-6 py-3 font-semibold text-blue-600 transition-colors hover:bg-gray-100"
        >
          Seguinos en Instagram
        </a>
        <a
          href="https://wa.me/1234567890"
          target="_blank"
          rel="noopener noreferrer"
          class="rounded-lg border-2 border-white bg-transparent px-6 py-3 font-semibold text-white transition-colors hover:bg-white hover:text-blue-600"
        >
          Dejá tu comentario
        </a>
      </div>
    </div>
  </div>
</section>
