---
import { SERVICES } from '@/consts/services';
import Icon from '@/components/Icon.astro';
---

<section id="servicios" class="bg-white py-20">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="mb-16 text-center">
      <h2 class="mb-4 text-3xl font-bold text-gray-900 md:text-4xl">
        Nuestros Servicios
      </h2>
      <p class="mx-auto max-w-3xl text-xl text-gray-600">
        Ofrecemos reparación profesional para todo tipo de dispositivos
        electrónicos con garantía y los mejores tiempos de entrega.
      </p>
    </div>

    <div class="mb-16 grid grid-cols-1 gap-8 md:grid-cols-2 lg:grid-cols-4">
      {
        SERVICES.map(service => {
          return (
            <div class="rounded-xl border border-gray-300 bg-gradient-to-br from-gray-50 to-white p-6 transition-all duration-300 hover:-translate-y-2 hover:transform hover:shadow-lg">
              <div class="mb-4 w-fit rounded-lg bg-blue-100 p-3">
                <Icon name={service.icon} class="h-8 w-8 text-blue-600" />
              </div>
              <h3 class="mb-3 text-xl font-semibold text-gray-900">
                {service.title}
              </h3>
              <p class="mb-4 text-gray-600">{service.description}</p>
              <ul class="grid gap-2">
                {service.features.map(feature => (
                  <li class="flex items-center gap-2 text-sm text-gray-600">
                    <div class="size-1.5 rounded-full bg-blue-600" />
                    {feature}
                  </li>
                ))}
              </ul>
            </div>
          );
        })
      }
    </div>

    <div
      class="rounded-2xl bg-gradient-to-r from-blue-600 to-purple-600 p-8 text-center text-white"
    >
      <div class="mb-4 flex items-center justify-center gap-3">
        <Icon name="shield" class="size-8" />
        <Icon name="wrench" class="size-8" />
      </div>
      <h3 class="mb-2 text-2xl font-bold">Garantía y Calidad Asegurada</h3>
      <p class="mb-6 text-blue-100">
        Todos nuestros trabajos incluyen garantía de 30 días. Utilizamos
        repuestos originales y de alta calidad para asegurar la mejor
        reparación.
      </p>
      <div class="flex items-center justify-center gap-8">
        <div class="text-center">
          <div class="text-3xl font-bold">30 días</div>
          <div class="text-blue-200">Garantía</div>
        </div>
        <div class="text-center">
          <div class="text-3xl font-bold">24-48h</div>
          <div class="text-blue-200">Entrega</div>
        </div>
        <div class="text-center">
          <div class="text-3xl font-bold">100%</div>
          <div class="text-blue-200">Original</div>
        </div>
      </div>
    </div>
  </div>
</section>
