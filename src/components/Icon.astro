---
import cn from '@/utils/cn';
import { Icon as AstroIcon } from 'astro-icon/components';

const { name, class: className = '', ...attrs } = Astro.props;
const classes = cn('size-6', className);
---

<AstroIcon name={name} class={classes} {...attrs} />

<!-- <svg class={classes} {...attrs}>
  <use xlink:href={`./src/assets/icons/sprite.svg#${name}`}></use>
</svg> -->
