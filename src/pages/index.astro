---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { ROUTES } from '@/consts/routes.ts';
---

<BaseLayout>
  {
    ROUTES.map(({ component: Component, id }) => (
      <Component id={id} class="bg-white nth-of-type-[odd]:bg-gray-50" />
    ))
  }
</BaseLayout>
